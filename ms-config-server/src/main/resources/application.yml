server:
  port: 7090

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/crhistianchuqui03/pas0.1.git # ruta del repositorio git
          searchPaths: config-data
          #username:
          #password:
          default-label: main


  security:
    user:
      name: root
      password: 123456